# 2026-06-10

## 数据目录迁移 & 全局路径配置

- 将 `image`、`image_yuntai`、`test_record` 三个文件夹迁移至 `D:\自动化测试保存\` 下
- 在 `config/model_config.py` 顶部新增全局路径配置：
  - `DATA_BASE_DIR = r"D:\自动化测试保存"`
  - `IMAGE_DIR`、`IMAGE_YUNTAI_DIR`、`TEST_RECORD_DIR` 三个常量
- 修改了以下文件中所有硬编码路径，统一改用全局配置：
  - `app.py`（导入 + read_result_file + write_result_file + serve_image + get_integrated_file + get_temperature_data）
  - `script/public.py`（download_latest_image + get_sn_from_ip + download_mos_file）
  - `script/dynamic.py`（_execute_hsr_yuntai + save_task_status）
  - `script/tw_mos.py`（save_temperature_data）
  - `script/pdf.py`（image_yuntai_path + __main__ test_file）
